pipeline {
    agent any

    stages {
        stage('Build Info') {
            steps {
                sh 'echo "Building Jenkins Demo Pipeline"'
                sh 'whoami'
                sh 'pwd'
            }
        }

        stage('List Files') {
            steps {
                sh 'ls -l'
            }
        }
    }
}

